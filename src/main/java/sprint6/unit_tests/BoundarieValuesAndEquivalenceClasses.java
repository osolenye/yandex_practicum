package sprint6.unit_tests;

import java.lang.reflect.Executable;

public class BoundarieValuesAndEquivalenceClasses {
    public static void main(String[] args) {
        // tests can take forever to run so we need to reduce them as much as possible but keeping them to test every value
        // boundary values - parameters that if we pass them program's behaviour changes
        // equivalence classes - parameters that don't change program's behaviour
        // we should test program on every boundary and one value from within boundaries
        // or every boundary and +/- 1 from the boundary

    }
}
